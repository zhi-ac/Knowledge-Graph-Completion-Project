# KGC项目核心业务流程图

## 1. 系统整体架构流程

```mermaid
graph TB
    subgraph "前端层"
        A[用户界面] --> B[导航菜单]
        B --> C[主页]
        B --> D[实体管理]
        B --> E[关系管理] 
        B --> F[三元组管理]
        B --> G[补全分析]
        B --> H[模型管理]
        B --> I[数据管理]
    end
    
    subgraph "后端API层"
        J[Flask路由] --> K[业务逻辑]
        K --> L[数据验证]
        L --> M[错误处理]
    end
    
    subgraph "数据层"
        N[内存缓存]
        O[MySQL数据库]
        P[HybridKnowledgeGraph]
    end
    
    subgraph "算法层"
        Q[TransE模型]
        R[嵌入向量]
        S[链接预测]
        T[三元组补全]
    end
    
    A --> J
    J --> P
    P --> N
    P --> O
    P --> Q
    Q --> R
    Q --> S
    Q --> T
```

## 2. 知识图谱初始化流程

```mermaid
sequenceDiagram
    participant U as 用户
    participant F as 前端
    participant A as API服务
    participant KG as 知识图谱
    participant DB as 数据库
    participant M as TransE模型
    
    U->>F: 点击初始化
    F->>A: POST /api/kg/init
    A->>KG: 创建HybridKnowledgeGraph
    KG->>DB: 尝试连接数据库
    alt 数据库可用
        DB-->>KG: 连接成功
        KG->>DB: 加载现有数据
    else 数据库不可用
        KG-->>KG: 使用内存模式
    end
    alt 需要示例数据
        KG->>KG: 创建示例三元组
    end
    KG->>M: 初始化TransE模型
    M->>M: 创建嵌入向量
    M-->>A: 模型就绪
    A-->>F: 返回初始化状态
    F-->>U: 显示图谱信息
```

## 3. 数据管理核心流程

```mermaid
flowchart TD
    A[数据操作请求] --> B{操作类型}
    
    B -->|添加三元组| C[验证参数]
    B -->|删除三元组| D[查找三元组]
    B -->|实体管理| E[实体CRUD]
    B -->|关系管理| F[关系CRUD]
    
    C --> G[添加到内存]
    D --> H[从内存删除]
    E --> I[更新数据库实体表]
    F --> J[更新数据库关系表]
    
    G --> K[同步到数据库]
    H --> K
    I --> L[重新加载知识图谱]
    J --> L
    
    K --> M[重建ID映射]
    L --> M
    M --> N[更新模型嵌入向量]
    N --> O[返回操作结果]
```

## 4. TransE模型训练流程

```mermaid
graph TD
    A[开始训练] --> B[获取知识图谱数据]
    B --> C[初始化嵌入向量]
    C --> D[生成负样本]
    D --> E[计算损失函数]
    E --> F{达到最大轮次?}
    
    F -->|否| G[梯度下降更新]
    G --> H[更新嵌入向量]
    H --> I[关系向量归一化]
    I --> D
    
    F -->|是| J[保存嵌入向量]
    J --> K[存储到数据库]
    K --> L[训练完成]
```

## 5. 知识图谱补全流程

```mermaid
stateDiagram-v2
    [*] --> 接收补全请求
    接收补全请求 --> 验证参数
    验证参数 --> 检查模型状态: 参数有效
    
    检查模型状态 --> 识别补全类型: 模型已训练
    识别补全类型 --> 尾实体预测: 缺少尾实体
    识别补全类型 --> 头实体预测: 缺少头实体
    
    尾实体预测 --> 计算候选实体
    头实体预测 --> 计算候选实体
    
    计算候选实体 --> 向量相似度计算
    向量相似度计算 --> 排序候选结果
    排序候选结果 --> 计算置信度
    计算置信度 --> 返回TopK结果
    返回TopK结果 --> [*]
    
    检查模型状态 --> 错误处理: 模型未训练
    验证参数 --> 错误处理: 参数无效
    错误处理 --> [*]
```

## 6. 链接预测流程

```mermaid
flowchart LR
    A[输入实体] --> B[获取实体ID]
    B --> C[获取实体嵌入向量]
    C --> D[遍历所有关系]
    D --> E[计算 head + relation]
    E --> F[计算与所有实体距离]
    F --> G[排序结果]
    G --> H[筛选TopK]
    H --> I[返回预测链接]
```

## 7. 混合存储架构流程

```mermaid
graph TB
    subgraph "客户端"
        A[用户请求]
    end
    
    subgraph "API层"
        B[Flask处理]
    end
    
    subgraph "业务逻辑层"
        C[HybridKnowledgeGraph]
    end
    
    subgraph "存储层"
        D[内存缓存]
        E[MySQL数据库]
    end
    
    subgraph "同步机制"
        F[双向同步]
        G[一致性检查]
    end
    
    A --> B
    B --> C
    
    C -->|读操作| D
    C -->|写操作| D
    C -->|持久化| E
    C -->|启动加载| E
    
    D <--> F
    E <--> F
    F --> G
    
    G -->|数据不一致| C
    C -->|重新同步| D
    C -->|重新同步| E
```

## 8. 系统状态流转

```mermaid
stateDiagram-v2
    [*] --> 未初始化
    未初始化 --> 初始化中: 用户点击初始化
    初始化中 --> 已初始化: 初始化成功
    初始化中 --> 初始化失败: 初始化错误
    
    已初始化 --> 模型训练中: 开始训练
    模型训练中 --> 模型就绪: 训练完成
    模型训练中 --> 已初始化: 训练失败
    
    模型就绪 --> 补全分析中: 执行补全
    补全分析中 --> 模型就绪: 补全完成
    
    已初始化 --> 数据管理中: 数据操作
    数据管理中 --> 已初始化: 操作完成
    
    模型就绪 --> 数据管理中: 添加新数据
    数据管理中 --> 需要重训练: 数据变化大
    需要重训练 --> 模型训练中: 重新训练
    
    初始化失败 --> 未初始化: 重新初始化
```

## 核心流程说明

### 关键特性：
1. **双模式存储**: 内存+数据库混合架构，保证性能和持久化
2. **动态扩展**: 支持运行时添加新实体、关系和三元组
3. **智能补全**: 基于TransE算法的知识图谱补全
4. **实时同步**: 内存和数据库数据实时一致性
5. **状态管理**: 完整的系统状态流转和错误处理

### 数据流向：
- **输入**: 用户通过Web界面或API输入数据
- **处理**: Flask路由处理业务逻辑，调用知识图谱和模型
- **存储**: 内存快速操作，数据库持久化存储
- **输出**: 可视化展示和API响应结果

### 算法核心：
- **TransE训练**: 负采样、损失计算、梯度下降
- **嵌入表示**: 实体和关系的向量空间映射
- **相似度计算**: 欧氏距离或余弦相似度
- **预测排序**: 基于向量相似度的候选实体排序